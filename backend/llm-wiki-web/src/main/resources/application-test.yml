spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MariaDB
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

jwt:
  secret: test-jwt-secret-key-at-least-256-bits-long-for-hs256-algorithm
  expiration: 86400000

ai:
  api:
    base-url: http://localhost:9999
    api-key: test-key
    model: test-model
  embedding:
    base-url: http://localhost:9999
    api-key: test-key
    model: test-embedding-model

scoring:
  threshold: 5.0

logging:
  level:
    root: WARN
    com.llmwiki: INFO
